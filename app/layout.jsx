export const metadata = {
  title: "HOME PROTECT",
  description: "Sistemas de seguridad y redes",
}

export default function RootLayout({ children }) {
  return (
    <html lang="es">
      <body>
        {children}
      </body>
    </html>
  )
}
